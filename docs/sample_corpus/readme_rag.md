# Локальный корпус RAG

Проект использует гибридный поиск: HNSW для семантики и BM25 для точных терминов.

Индекс хранится в `.rag_index` внутри папки корпуса.

Параметр overlap задаётся в `config/rag_config.json`.
